# Funções de Entrada e Saída em Python

As funções de **entrada** e **saída** permitem que o programa interaja com o usuário.

- **Entrada** → Receber dados do usuário.
- **Saída** → Mostrar informações na tela.

---

# 1️⃣ Função de Saída: `print()`

A função `print()` é utilizada para exibir informações no console.

## Sintaxe básica

```python
print(valor)
```

---

## 📌 Parâmetros da função `print()`

A função `print()` possui alguns parâmetros importantes:

### 🔹 `sep` (separador)

Define o que será usado para separar múltiplos valores.

📍 Padrão: espaço `" "`

```python
print("Python", "é", "top")
```

Saída:
```
Python é top
```

Alterando o separador:

```python
print("2026", "02", "28", sep="-")
```

Saída:
```
2026-02-28
```

---

### 🔹 `end` (final da linha)

Define o que será exibido ao final da impressão.

📍 Padrão: quebra de linha `\n`

Exemplo padrão:

```python
print("Olá")
print("Mundo")
```

Saída:
```
Olá
Mundo
```

Alterando o `end`:

```python
print("Olá", end=" ")
print("Mundo")
```

Saída:
```
Olá Mundo
```

Outro exemplo:

```python
print("Carregando", end="...")
```

Saída:
```
Carregando...
```

---

## 🧠 Usando `sep` e `end` juntos

```python
print("10", "20", "30", sep=" - ", end=" Fim")
```

Saída:
```
10 - 20 - 30 Fim
```

---

# 2️⃣ Função de Entrada: `input()`

A função `input()` é utilizada para receber dados do usuário.

## Sintaxe

```python
variavel = input("Mensagem para o usuário: ")
```

## Exemplo

```python
nome = input("Digite seu nome: ")
print("Olá,", nome)
```

⚠️ Importante:  
O `input()` sempre retorna uma **string (`str`)**.

---

# 3️⃣ Convertendo Tipos com `input()`

Se quisermos receber números, precisamos converter o valor.

## Converter para inteiro

```python
idade = int(input("Digite sua idade: "))
```

## Converter para decimal

```python
altura = float(input("Digite sua altura: "))
```

---

# 4️⃣ Exemplo Completo

```python
nome = input("Digite seu nome: ")
idade = int(input("Digite sua idade: "))

print("Nome:", nome)
print("Idade:", idade)
print("Ano que vem você terá", idade + 1, "anos.")
```

---

# 5️⃣ Resumo

| Função / Parâmetro | Função no Programa |
|--------------------|-------------------|
| print()            | Exibe informações na tela |
| input()            | Recebe dados do usuário |
| sep                | Define o separador entre valores |
| end                | Define o final da linha |
| int()              | Converte para número inteiro |
| float()            | Converte para número decimal |

---

# 6️⃣ Boas Práticas

- Sempre converta `input()` quando precisar de números.
- Use mensagens claras dentro do `input()`.
- Use `sep` para organizar melhor a saída.
- Use `end` quando quiser controlar a quebra de linha.
- Evite misturar texto e número sem conversão.